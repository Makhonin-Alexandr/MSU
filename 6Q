n = int(input())

standard_deviation = 0
sum = 0
count = 0

while n != 0:
    count += 1
    sum += n
    standard_deviation += n ** 2
    n = int(input())

standard_deviation = ((standard_deviation - sum ** 2 / count) / (count - 1)) ** 0.5
print(standard_deviation)
